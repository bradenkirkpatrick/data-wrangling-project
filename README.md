# Running our File

#### Create venv (optional)

python3 -m venv .venv
source ./.venv/bin/activate

#### Install requitrements

pip install -r requirements.txt

#### Run (in Terminal)

python main.py

#### Use CSV Files

A wide format "wide_batting_stats.csv" and "batting_stats_calculations.csv" should appear in the "saves" folder.
